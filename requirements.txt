# Build-time deps only — gradio-compatible, no transformers version conflict.
# The exact model-required versions are pip-installed at runtime in app.py.
matplotlib>=3.7.0
einops>=0.8.0
addict>=2.4.0
easydict>=1.13
pymupdf>=1.27.0
psutil>=5.9.0
Pillow>=10.0.0
spaces
gradio
